exchange:
   kraken:
     api_key: ${KRAKEN_API_KEY}
     rate_limit:
       calls_per_second: 10

strategy:
  momentum:
    lookback: 20
    threshold: 0.02

risk:
  max_position_size: 0.10
  max_drawdown: 0.20
  stop_loss: 0.05
